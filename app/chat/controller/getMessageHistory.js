const chatService = require('../service/chatService');module.exports = (req, res, next) => {  // res.json({code:0,msg:'登陆ok!'});  let phone=req.query.phone || req.body.phone;  if(!phone){    res.json({code:-1,msg:'手机号不能为空'});  }  chatService.getMsgHistory(phone,(msgList)=>{    if(msgList){      Tools.json(res,0,'获取聊天历史成功',msgList);    }else{      Tools.json(res,-1,'失败');    }  })}